function [ model ] = naive_bayes(trn_dat,trn_lab)
% Naive Bayes
% sontran 2013
[M N] = size(trn_dat);
PY = struct
PX = repmat
% Get range
PY.var = 0;
PY.cad
Py,prob = zeros(1,M);
for i=1:N
     unique(trn_dat(:,i));
end



end

